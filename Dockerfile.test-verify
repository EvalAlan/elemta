FROM golang:1.24-alpine AS builder

WORKDIR /build

RUN apk add --no-cache git gcc musl-dev

COPY go.mod go.sum ./
RUN go mod download

COPY . .

# Build check
RUN CGO_ENABLED=0 go build ./...

# Vet check
RUN go vet ./...

# Run tests
RUN go test -v -count=1 ./... 2>&1 | tee /tmp/test-results.txt
