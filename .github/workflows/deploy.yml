# Placeholder only: no target environment is provisioned (0 GitHub Environments
# configured for this repo as of 2026-09-25). This does not deploy anything.
# Real deployment today is manual — see docs/production-deployment.md.
# Wire this up for real once an operator names and provisions a target
# environment (see EVI-11).
name: Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy to"
        required: true
        default: "staging"
        type: choice
        options:
          - staging
          - production

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Echo deploy target
        run: |
          echo "Would deploy elemta to ${{ github.event.inputs.environment }} here."
          echo "Wire this to your real deploy (e.g. helm, terraform, ssh, etc.)."